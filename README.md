# RunDo AI + Energy Presentation

Private presentation source. Open `index.html` locally to view the bilingual presentation.

Any online deployment must require authentication before serving the HTML, which contains embedded product screenshots. A private repository alone does not make its deployed website private.
